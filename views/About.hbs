<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About Us - Coin2Flow</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <!-- FontAwesome for Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: black;
      color: #ffffff;
      font-family: 'Poppins', sans-serif;
    }

    .header {
      background-color: black;
      padding: 15px 20px;

    }

    .header a {
      color: #ffffff;
      text-decoration: none;
      margin-right: 20px;
    }

    .header a:hover {
      color: #ffc107;
    }


    #logo {
      width: 150px;
      height: 70px;

    }

    .intro {
      font-size: 20px;
      font-family: "lato", sans-serif;
      font-weight: 500;
      color: #fff;

    }

    .team-card {
      background-color: #1f1f1f;
      border: 1px solid #28a745;
      border-radius: 10px;
      transition: transform 0.3s, box-shadow 0.3s;
    }



    .team-card img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      margin-bottom: 15px;
    }

    .team-card h5 {
      color: #ffc107;
    }

    .footer {
      background-color: #1f1f1f;
      color: #ffffff;
      padding: 15px 0;
      border-top: 2px solid #28a745;
    }

    .message-form {
      background-color: #1f1f1f;
      border: 1px solid #ffc107;
      border-radius: 10px;
      padding: 20px;
      animation: fadeIn 0.8s;
    }

    .message-form button {
      background-color: #28a745;
      border: none;
      transition: background-color 0.3s, transform 0.3s;
    }

    .message-form button:hover {
      background-color: #218838;
      transform: scale(1.1);
    }

    .message-form .alert {
      display: none;
      margin-top: 10px;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }
  </style>
</head>

<body>
  <!-- Header -->
  <header class="header d-flex justify-content-between align-items-center">
    <img src="/assets/logo.bank.png" alt="Coin2Flow" height="50" id="logo">
    <nav>
      <a href="/dashboard" class="btn btn-success">Home</a>
      <!-- <a href="services.html">Services</a>
      <a href="contact.html">Contact</a>
      <a href="about.html">About Us</a> -->
    </nav>
  </header>

  <!-- About Us Section -->
  <section class="container my-5">
    <div class="text-center mb-5">
      <h2 class="text-warning introh">Meet the Team</h2>
      <p class=" intro">The Masterminds behind Coin2Flow - An Innovative Online Banking system.</p>
    </div>
    <div class="row g-4">
      <div class="col-md-4">
        <div class="team-card p-3 text-center">
          <img src="/assets/shadil.jpg" alt="Shadil">
          <h5>Shadil</h5>
          <p>Lead Developer and Backend Specialist.</p>
          <a href="shadilrock77@gmail.com" class="btn btn-success btn-sm mt-2">
            <i class="fas fa-envelope"></i> Contact
          </a>
        </div>
      </div>
      <div class="col-md-4">
        <div class="team-card p-3 text-center">
          <img src="/assets/ritik.jpg" alt="Ritik">
          <h5>Ritik</h5>
          <p>Frontend Developer and UI/UX Designer.</p>
          <a href="ritikdr31@gmail.com" class="btn btn-success btn-sm mt-2">
            <i class="fas fa-envelope"></i> Contact
          </a>
        </div>
      </div>
      <div class="col-md-4">
        <div class="team-card p-3 text-center">
          <img src="/assets/sami.jpg" alt="Abdul">
          <h5>Abdul</h5>
          <p>Database Manager and System Integrator.</p>
          <a href="mailto:abdul@example.com" class="btn btn-success btn-sm mt-2">
            <i class="fas fa-envelope"></i> Contact
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Message Section -->
  <section class="container my-5">
    <div class="text-center mb-4">
      <h3 class="text-warning">Get in Touch</h3>
      <p>Have a question or feedback? Write to us!</p>
    </div>
    <form id="messageForm" class="message-form mx-auto" style="max-width: 600px;">
      <div class="mb-3">
        <label for="name" class="form-label">Name</label>
        <input type="text" class="form-control" id="name" placeholder="Your Name">
      </div>
      <div class="mb-3">
        <label for="email" class="form-label">Email</label>
        <input type="email" class="form-control" id="email" placeholder="Your Email">
      </div>
      <div class="mb-3">
        <label for="message" class="form-label">Message</label>
        <textarea class="form-control" id="message" rows="4" placeholder="Your Message"></textarea>
      </div>
      <div class="text-center">
        <button type="submit" class="btn btn-success" id="sendMsg">Send Message</button>
        <div class="alert alert-success mt-3" id="successAlert">Message Sent Successfully!</div>
        <div class="alert alert-danger mt-3" id="errorAlert">Failed to send the message. Please try again.</div>
      </div>
    </form>
  </section>


  <footer class="footer text-center">
    <p class="m-0">&copy; 2024 Coin2Flow. All rights reserved.</p>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
  <script>
    document.getElementById("messageForm").addEventListener("submit", async (e) => {
      e.preventDefault();

      const name = document.getElementById("name").value;
      const email = document.getElementById("email").value;
      const message = document.getElementById("message").value;

      try {
        const response = await fetch("/auth/MessageAdmin", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ name, email, message }),
        });

        const data = await response.json();

        if (response.ok) {
          document.getElementById("successAlert").style.display = "block";
          setTimeout(() => {
            document.getElementById("successAlert").style.display = "none";
          }, 3000);
        } else {
          throw new Error(data.message || "Failed to send the message.");
        }
      } catch (err) {
        console.error("Error:", err);
        const errorAlert = document.getElementById("errorAlert");
        errorAlert.style.display = "block";
        setTimeout(() => {
          errorAlert.style.display = "none";
        }, 5000);
      }
    });

  </script>